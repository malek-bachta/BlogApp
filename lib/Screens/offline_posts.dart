import 'package:flutter/material.dart';

class OfflinePosts extends StatelessWidget {
  const OfflinePosts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
